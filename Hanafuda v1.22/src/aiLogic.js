// src/aiLogic.js

export function chooseCardToPlay(hand, field) {
  // Return which card the AI picks
  // e.g. random or something more clever
}

export function decideKoikoiOrStop(currentPoints, gameState) {
  // Basic logic: if the AI is ahead => maybe Stop, else Koikoi
  return (someCondition) ? 'koikoi' : 'stop';
}